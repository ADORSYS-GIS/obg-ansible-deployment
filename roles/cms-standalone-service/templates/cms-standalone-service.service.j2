[Unit]
Description=ASPSP Profile Server
After=network.target

[Service]
User=obg
ExecStart=/usr/bin/java -jar /opt/cms-standalone-service/cms-standalone-service.jar
Restart=on-failure

[Install]
WantedBy=multi-user.target
